# VoiceCAP 무료 로컬 STT 도입 계획 및 AI 작업 인계서

- 작성일: 2026-09-04
- 프로젝트: `C:\dev\voicecap-web`
- 상태: 조사 및 계획 완료. STT 기능 구현은 아직 시작하지 않음.
- 사용자 요청: 라이브 청취 홈에 기존 STT API와 함께 선택할 수 있는 무료 오프라인 STT를 추가하고, 한국어 인식률이 높은 모델과 구현 방안을 제안한다.
- 이 문서는 보고한 계획을 다른 AI가 이어서 구현할 수 있도록 정리한 것이다. 저장 자체는 구현·배포 완료를 의미하지 않는다.

## 1. 추천 및 도입 방향

**1차 기본 모델은 Whisper large-v3-turbo, 실행 엔진은 faster-whisper를 추천한다.**

기존 Windows 댓글 도우미에 로컬 음성인식 프로세스를 추가하고, 라이브 청취 홈에서 클라우드 또는 내 PC 무료 STT를 선택하게 한다. Windows 설치 편의성과 실시간 처리 속도를 고려한 추천이며, 한국어 정확도 최상위라는 실측 결론은 아니다.

한국어 정확도 비교 후보로 Whisper large-v3와 Qwen3-ASR-1.7B를 평가한다. 실제 판매 방송에서 Qwen의 정확도 이점이 충분하고 배포 조건을 충족하면 후속 옵션으로 추가한다.

| 후보 | 장점 | 고려 사항 | 권장 용도 |
| --- | --- | --- | --- |
| Whisper large-v3-turbo | 다국어 인식과 처리 속도의 균형 | 한국어 방송에서 정확도·지연 실측 필요 | 1차 기본 모델 |
| Whisper large-v3 | 정확도를 우선해 비교할 후보 | Turbo보다 처리 부담이 큼 | 고성능 PC의 정확도 우선 옵션 |
| Qwen3-ASR-1.7B | 한국어 지원, 강한 다국어 인식 성능 | 공식 스트리밍 경로의 Windows 배포가 복잡함 | 정확도 중심 비교 후보 |
| Vosk 한국어 모델 | 공식 한국어 소형 모델 약 82MB, 가벼운 실행 | 닉네임·금액 인식에 대한 별도 검증 필요 | 저사양 보조 후보 |

Qwen 개발팀 보고서의 한국어 포함 다국어 평균 성능을 한국어 라이브 판매 방송의 정확도로 해석하면 안 된다. 조사 당시 Qwen 공식 스트리밍은 vLLM 백엔드를 사용하며, vLLM은 Windows 네이티브를 지원하지 않아 WSL 등의 구성이 필요했다. 구현 시작 시 지원 현황을 다시 확인한다.

여기서 무료·오프라인은 **STT API 사용료 없이 사용자 PC에서 음성을 처리한다**는 뜻이다. 최초 모델 설치에는 다운로드 또는 모델 파일 반입이 필요하다. 라이브 방송 수신, 로그인, 댓글 수집 등 기존 온라인 기능까지 인터넷 없이 동작하게 만드는 것은 이번 범위가 아니다.

## 2. 조사한 현재 코드 구조

구현 AI는 최신 코드를 다시 확인하고 아래 경로와 상태가 유효한지 점검한다. 다른 작업자의 변경을 덮어쓰지 않는다.

| 파일 / 디렉터리 | 현재 역할 | 예정 변경 |
| --- | --- | --- |
| `src/pages/seller/LiveHomePage.tsx` | 라이브 청취 홈, STT 상태와 API 키 설정 표시 | 클라우드/로컬 선택기, 설치·모델·연결 상태 UI |
| `src/context/LiveContext.tsx` | 오디오와 STT 시작·중지, 전사 처리, 판매·캡처·음성 수정 실행 | 선택 엔진 연결, 공통 결과 처리, 세션 안전성 유지 |
| `src/services/deepgramService.ts` | 이름과 달리 Deepgram·Soniox 및 Web Speech 대체 경로 처리 | 공급자 공통 인터페이스 분리 및 기존 서비스 어댑터화 |
| `src/services/audioCaptureService.ts` | 탭·마이크 오디오를 16kHz 모노 PCM16으로 전달 | 기존 수집 재사용, 실제 샘플레이트 및 전송 준비 상태 검증 |
| `src/types/deepgram.ts` | `SttProvider = 'DEEPGRAM' \| 'SONIOX'` 및 공급자 설정 | 로컬 공급자와 공급자별 설정 타입 지원 |
| `src/types/live.ts` | 전사 로그 및 판매 관련 타입 | 필요 시 발화 ID·시각·신뢰도 미제공 상태 지원 |
| `src/services/storageService.ts` | STT 선택·키 저장 및 백업/복원 | 로컬 선택·모델 설정 저장, 기존 데이터 호환 |
| `src/pages/admin/AdminDashboardPage.tsx` | 클라우드 기본 STT 선택 및 API 키 관리 | 클라우드 기본 설정과 PC별 로컬 선택의 관계 정리 |
| `desktop/comment-helper/` | Windows Electron 설치형 댓글 도우미 | 로컬 STT 프로세스 관리·설치·모델 관리 확장 |
| `server/index.js` | 도우미 로컬 서버와 웹 연결 | 필요한 로컬 STT 통신 경로 및 접근 검증 |

확인한 동작:

- 오디오 수집은 현재 ScriptProcessor 기반으로 PCM16 청크를 전달한다.
- 기존 전사 콜백에는 `text`, `isFinal`, `confidence`, `provider` 등이 있다.
- `LiveContext`는 확정 전사로 판매 저장·음성 수정·화면 캡처를 실행한다. Soniox에는 별도 누적 처리도 존재한다.
- 세션 generation 및 사용자 ID로 이전 청취 결과가 새 세션에 영향을 주지 않도록 방어한다.
- 탭 청취 중지 시 공유 연결을 유지하는 로직이 존재하므로 이를 보존해야 한다.
- 현재 STT 공급자 설정은 localStorage에 저장되며 백업/복원에도 포함된다. 주석이나 UI의 '전역' 표현만으로 서버 공통 설정이라고 가정하지 않는다.
- 도우미는 설치, 백그라운드 실행, 업데이트 구조를 이미 갖추고 있다.

## 3. 목표 구조

```text
방송 탭 / 마이크
      ↓
기존 오디오 수집 (16kHz, mono, PCM16)
      ↓
공통 STT 서비스 / 공급자 선택
      ├─ 클라우드 → 기존 Deepgram / Soniox
      └─ 로컬 → PC의 도우미 → 별도 faster-whisper 프로세스
                                ↓
                       공통 전사 결과 처리
                                ↓
                    자막 · 판매 등록 · 화면 캡처
```

- 모델 추론은 사용자 PC에서 실행한다. 원격 서버에 모델을 올리는 방식은 이번 로컬 STT 요구를 충족하지 않는다.
- 무거운 추론을 Electron UI나 댓글 수집 이벤트 루프에서 직접 실행하지 않는다.
- STT 프로세스는 모델을 메모리에 유지하고 세션마다 재다운로드·재로딩하지 않도록 한다.
- 모델 준비 후 청취를 시작하거나 초기 청크를 제한된 버퍼에 보관하여 시작 부분을 잃지 않게 한다.
- 로컬 서버는 루프백에 바인딩하고 허용된 Origin 및 연결 토큰을 검증한다. CORS만으로 WebSocket 접근 제어가 충분하다고 가정하지 않는다.
- 실제 배포된 HTTPS 페이지와 지원 브라우저에서 로컬 연결, 로컬 네트워크 권한 및 연결 실패 안내를 검증한다.

## 4. 구현 단계

### 단계 1 — 실제 한국어 방송과 PC 성능 비교

가능하면 사용자가 제공하거나 사용 권한이 있는 한국어 판매 방송 녹음을 확보한다. 없으면 테스트 문장을 녹음하는 절차를 마련하고 평가가 미완료임을 명시한다. 라이브 자막만으로 정답 전사라고 가정하지 않는다.

- 같은 오디오로 기존 API, Turbo, large-v3, Qwen3-ASR-1.7B를 비교한다. 기존 API를 실제 호출하는 평가는 키·비용·오디오 전송에 대한 사용자의 실행 요청 범위를 따른다.
- 문자 오류율(CER), 닉네임·금액·수량 정확 일치율, 명령 누락·오작동을 측정한다.
- 띄어쓰기 정규화 기준을 고정하고, CER과 업무 필드 정확도를 구분한다.
- 발화 종료부터 확정까지의 지연, 처리 지연 누적, 방송 프로그램 동시 실행 시 자원 사용량을 측정한다.
- GPU 사용 가능 여부, 모델 로딩 시간, 메모리 사용량, CPU 실행 성능을 기록한다.
- 공개 벤치마크나 처리량 수치를 사용자 PC의 단일 라이브 지연으로 환산하지 않는다.

### 단계 2 — 공급자 공통 인터페이스 및 설정 분리

- 공통 STT 타입과 서비스 진입점을 만든다. 예: `src/types/stt.ts`, `src/services/sttService.ts`, `src/services/localSttService.ts`. 경로는 제안이며 기존 구조에 맞게 결정한다.
- 공통 동작은 시작, 오디오 전달, 중지, 상태, 전사, 오류로 정의한다.
- 클라우드 API 키와 로컬 모델·장치 설정을 공급자별로 구분한다. 로컬 선택 때문에 API 키 검사가 실패하지 않게 한다.
- 기존 클라우드 기본값은 유지하고, 실제 청취 모드와 로컬 설정은 PC별 선택으로 저장한다.
- 기존 두 공급자만 가정한 삼항식, 타입, 화면 문구, 백업/복원 검사를 전수 확인한다.
- 이전 localStorage 및 백업 파일을 계속 읽을 수 있게 한다.

### 단계 3 — 도우미 로컬 STT 구현 및 배포

- 별도 faster-whisper 실행 프로세스를 도우미에서 시작·종료·감시한다.
- Python 런타임과 필요한 라이브러리의 배포 방법을 정하고, 일반 사용자가 Python·pip·환경변수·포트를 직접 다루지 않게 한다.
- 최초 모델 다운로드 진행률, 실패·재시도, 파일 무결성, 저장 공간, 모델 버전을 관리한다.
- 모델 다운로드/설치 단계와 추론 단계를 분리하고, 추론 시에는 로컬 모델 경로만 사용한다.
- CPU·GPU 지원과 필요한 런타임을 확인한다. GPU가 없거나 초기화가 실패한 상태를 사용자에게 표시한다.
- 시작/준비/오디오/전사/중지 프로토콜에 세션 ID, 발화 ID, 오디오 시각을 포함하도록 설계한다.
- 전송·추론 큐에 상한을 두고, 지연 누적·프로세스 종료·연결 끊김을 명시적으로 처리한다. 오디오를 조용히 유실시키지 않는다.
- 배포할 모델·실행 라이브러리의 실제 라이선스 및 고지 파일을 확인해 패키지에 포함한다.

### 단계 4 — 연속 전사와 한국어 처리

Whisper 자체를 무제한 실시간 스트리밍 엔진으로 가정하지 말고, 음성 구간 감지(VAD)와 청크 처리로 연속 전사를 구현한다.

- 초기 조정값: 약 2~4초 구간 처리, 약 0.5~0.8초 무음에서 발화 확정. 성능 보장값이 아니며 실측으로 조정한다.
- 구간 겹침을 활용해 단어 잘림을 줄이고, 겹친 전사 중복을 제거한다.
- 실제로 반복 발화한 같은 문장까지 문자열만으로 제거하지 않는다. 오디오 구간과 발화 ID를 함께 사용한다.
- 한국어 전사로 고정하고 번역 동작이 발생하지 않도록 한다.
- 상품명·음성 명령·등록 규칙은 지원되는 문맥 힌트로 사용하되, 무음에 힌트 문장이 생성되는 등 부작용을 검사한다.
- 중간 결과는 자막으로만 표시하고, 확정 결과만 판매·캡처·음성 수정 처리로 전달한다.
- 연속 발화의 강제 구간 분할 때문에 닉네임과 금액이 다른 구간으로 갈라지는 경우를 검증하고 필요하면 공통 업무 문장 누적 처리를 추가한다.

### 단계 5 — 업무 동작과 세션 안전성

- 세션·발화 ID로 확정 결과의 업무 처리를 한 번만 수행한다.
- 청취 중지, 재시작, 로그아웃, 계정 변경 이후 늦게 도착한 결과를 폐기한다.
- 탭 공유 연결 유지 및 재사용 동작을 보존한다.
- 정상 중지 시 잔여 발화 확정 정책과 취소/로그아웃 시 폐기 정책을 구분한다. 중지 후 늦은 결과로 판매가 생성되지 않게 한다.
- 전사 지연으로 '캡처하세요' 발화 당시 화면과 캡처 결과가 달라지는 문제를 측정한다.
- 필요하면 발화 시각에 대응하는 짧은 화면 버퍼를 추가한다. 시간 기준, 보관 길이, 메모리 상한, 세션 종료 시 정리를 명시하고 실측 후 필요성을 결정한다.
- 엔진이 신뢰도를 제공하지 않으면 '미제공'으로 표현한다. 임의의 95% 등의 값을 생성하거나 엔진의 확률 점수를 검증된 정확도로 표시하지 않는다.
- 로컬 장애 시 오류와 재시도 안내를 제공한다. 유료 클라우드나 브라우저 마이크로 자동 전환하지 않는다.

### 단계 6 — 라이브 청취 홈 UI

청취 시작 버튼 근처에 다음 선택기를 둔다.

```text
음성인식 방식: [기존 클라우드 STT] [내 PC 무료 STT]
```

로컬 모드 표시:

- 도우미 미설치 / 연결 끊김 / 연결됨
- 모델 미설치 / 다운로드 중 / 로딩 중 / 준비됨
- 선택 모델과 GPU 또는 CPU 사용 상태
- 성능 부족 또는 처리 지연 안내
- 설치·다운로드·재시도 동작

클라우드 모드에서는 기존 공급자와 API 키 설정 흐름을 유지한다. 로컬 모드에는 API 키 입력을 요구하지 않는다. 청취 중 모드 변경은 중지 후 적용하도록 한다.

모델 구성은 초기 배포에서 과도하게 늘리지 않는다. GPU에서는 Turbo를 우선 검증하고, CPU에서는 small 또는 medium의 양자화 실행을 비교한다. 저사양 모델은 한국어 정확도 저하 가능성을 명시한다. 측정 전 최소 사양이나 지연 시간을 확정하지 않는다.

## 5. 검증 및 완료 기준

기능 구현 AI는 아래 항목을 검증하고, 실측하지 못한 항목은 완료로 보고하지 않는다.

1. 라이브 청취 홈에서 클라우드와 로컬 모드를 선택하고 해당 PC에서 선택이 유지된다.
2. 로컬 모드는 API 키 없이 방송 탭과 마이크 오디오를 전사한다.
3. 모델 설치 이후 외부 통신을 차단한 상태에서도 로컬 파일/마이크 입력으로 전사한다. 로그인이나 방송 수신이 필요한 전체 앱 흐름과 로컬 추론 테스트를 구분한다.
4. 로컬 전사 오디오가 외부 STT 서버로 전송되지 않는다. 오류 시에도 클라우드로 자동 전환되지 않는다.
5. 중간 결과로 판매나 캡처를 실행하지 않는다. 겹친 구간·재전송으로 업무 동작이 중복되지 않는다.
6. 닉네임·금액·수량·구매확정·캡처·음성 수정 문장에 대한 평가 결과를 기록한다.
7. 1시간 연속 청취에서 지연과 메모리가 무제한으로 증가하지 않는다.
8. 도우미 미설치, 모델 없음, 다운로드 실패, GPU 실패, 연결 끊김, 프로세스 종료를 처리한다.
9. 중지·재시작·로그아웃·계정 변경 후 이전 전사 결과가 적용되지 않는다.
10. 기존 Deepgram·Soniox, 탭 공유 유지, 댓글 수집, 판매 등록 및 캡처 동작에 회귀가 없다.
11. 실제 배포 HTTPS 페이지에서 도우미 연결 및 브라우저 권한 흐름을 확인한다.
12. 깨끗한 Windows 환경에서 설치 패키지를 검증한다. 개발 PC에만 설치된 런타임에 의존하지 않는다.
13. `npm run build`와 변경 범위에 해당하는 기존 테스트를 실행한다. 추가 테스트는 중복 처리·세션 격리·오류 복구 등 실제 위험을 검증하도록 작성한다.

정확도와 지연의 최종 합격 수치는 실제 녹음과 대상 PC 비교 후 정한다. '한국어 99%' 등의 근거 없는 수치로 완료를 주장하지 않는다.

## 6. 예상 일정과 산출물

Windows 1차 버전 기준 약 5~8개 개발일의 초기 추정이다. 대상 PC 사양, 모델 패키징 및 설치 문제에 따라 변경될 수 있다.

| 작업 | 초기 추정 |
| --- | --- |
| 한국어 정확도 및 성능 비교 | 1~2일 |
| 로컬 서비스·공통 인터페이스·홈 UI 연결 | 2~3일 |
| 설치 패키징·장시간 검증 | 2~3일 |

산출물:

- 로컬 STT를 선택할 수 있는 웹 코드
- 로컬 STT가 포함된 도우미 코드 및 재현 가능한 빌드 절차
- 모델 설치·관리 및 오류 복구 UI
- 한국어 정확도·지연·자원 사용량 비교 결과
- 설치 및 검증 보고서, 알려진 제한 사항

코드 변경 및 로컬 설치 파일 생성과 운영 배포를 구분한다. GitHub Release 업로드, 웹 운영 배포 등 외부 공개 작업은 그 시점의 사용자 요청 및 프로젝트 지침에 따라 수행한다.

## 7. 참고 자료

아래는 계획 작성 시 확인한 공식 문서 또는 개발팀의 1차 자료다. 버전 및 플랫폼 지원은 구현 시 재확인한다.

- Whisper Turbo 모델 설명: https://huggingface.co/openai/whisper-large-v3-turbo
- faster-whisper 공식 저장소: https://github.com/SYSTRAN/faster-whisper
- Whisper 공식 저장소 및 라이선스 안내: https://github.com/openai/whisper
- Qwen3-ASR 공식 저장소와 스트리밍 조건: https://github.com/QwenLM/Qwen3-ASR
- Qwen3-ASR-1.7B 모델: https://huggingface.co/Qwen/Qwen3-ASR-1.7B
- Qwen3-ASR 기술 보고서: https://arxiv.org/html/2601.21337v1
- vLLM 설치 및 플랫폼 조건: https://docs.vllm.ai/en/latest/getting_started/installation/gpu/
- Vosk 공식 모델 목록: https://alphacephei.com/vosk/models
- 브라우저 Whisper WASM 예제와 제약: https://github.com/ggml-org/whisper.cpp/blob/master/examples/whisper.wasm/README.md
- Web Speech 로컬 처리 설정: https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition/processLocally

참고: 기존 Web Speech 대체 경로를 무료 오프라인 옵션으로 간주하지 않는다. 로컬 처리는 브라우저 지원, 명시적 로컬 설정, 한국어 언어팩 사용 가능 여부를 별도로 확인해야 한다. 이번 1차 구현은 Windows 도우미 기반으로 진행한다.

## 8. 다른 AI에게 전달할 작업 요청 예시

> 루트의 `OFFLINE_STT_IMPLEMENTATION_PLAN.md`를 읽고 무료 로컬 STT 선택 기능을 구현해라. 최신 코드와 적용되는 프로젝트 지침을 먼저 확인하고, 기존 클라우드 STT와 탭 공유 유지 동작을 보존해라. 1차 엔진은 faster-whisper와 Whisper large-v3-turbo이며, 기존 Windows 댓글 도우미를 확장해 사용자 PC에서 실행하게 해라. 한국어 정확도·실시간 지연·중복 업무 처리·세션 격리·설치 패키지를 검증하고, 실측 결과와 미완료 항목을 구분해 보고해라. 운영 배포는 별도 요청 범위에 따라 처리해라.
