# 무료 로컬 STT 인식 실패·반복 출력 분석 보고서

- 분석일: 2026-09-04
- 기준 소스: `39dc2d0` 및 현재 작업 트리
- 대상: `HANDOFF.md` v2.0.0, 웹 로컬 STT 서비스, Node 브리지, Python 워커, 설치된 Windows 도우미
- 요청 증상: 한국어 인식이 거의 되지 않음(사용자 체감 약 1%), 한 글자가 약 100회 반복된 사례
- 작업 범위: 코드·실행 상태 분석, 격리된 진단, 개선 방안 보고. 운영 코드나 실행 중 도우미 설정은 변경하지 않았다.

## 1. 결론

**모델 선택·오류 상태·음성 구간 분할·반복 결과 검증·실시간 처리 제어에 복수의 결함이 있다. 모델만 크게 바꾸는 방식으로 해결하기 어렵다.**

현재 PC는 Celeron J4105, Intel UHD 600, 약 8GB RAM이며 조사 순간 가용 물리 메모리는 약 0.85GiB였다. 실행 중 브리지는 `base / cpu / int8`을 보고했으며, `모델 로딩 실패: mkl_malloc: failed to allocate memory` 오류가 남아 있는데도 `LISTENING` 상태였다. 이 하드웨어에서 large-v3-turbo로 무조건 상향하는 것을 우선 처방으로 권하지 않는다.

반복 출력은 현재 워커에서 그대로 통과한다. 모의 디코더 결과를 사용한 격리 진단에서 한 글자 100회 반복이 확정 자막으로 전송되고 신뢰도 0.90으로 표시되는 것을 확인했다. 이는 반복 결과 차단 부재를 재현한 것이며, 사용자의 실제 음성에서 같은 문자가 생성되는 현상을 재현한 것은 아니다.

실제 문제 음성·당시 입력 설정·원시 전사 로그는 아직 확보하지 못했다. 따라서 체감 1%를 측정된 인식률로 확정하거나, 하나의 원인이 모든 증상을 설명한다고 단정하지 않는다.

## 2. 조사 방법 및 관측값

### 실행 환경 확인

- CPU: Intel Celeron J4105 @ 1.50GHz, 4코어/4논리 프로세서
- GPU: Intel UHD Graphics 600
- OS 보고 메모리: 8,204,312KiB 총 물리 메모리, 890,904KiB 가용 물리 메모리(조사 순간 값)
- Python 환경: 도우미가 탐색한 Hermes venv
- 설치 라이브러리: faster-whisper 1.2.1, CTranslate2 4.8.0
- 모델 캐시 최상위 폴더: base, small 확인. 폴더 존재만으로 모델 다운로드 완전성을 보장하지 않는다.
- 상태 API: `GET http://127.0.0.1:2137/api/stt/status`

API 관측 핵심값:

```json
{
  "available": true,
  "state": "LISTENING",
  "model": "base",
  "device": "cpu",
  "computeType": "int8",
  "message": "로컬 STT 준비 완료 (base / cpu int8)",
  "error": "모델 로딩 실패: mkl_malloc: failed to allocate memory"
}
```

오류 발생 시각과 실패한 모델명은 제공되지 않는다. 따라서 이 오류가 조사 순간 발생한 것인지, 어떤 모델의 로딩 실패인지 확정할 수 없다. 코드상 오래된 오류가 남을 수 있으므로 현 상태와 마지막 오류를 구분해야 한다.

### 설치본 대조

실행 중 프로세스의 워커 경로를 확인하고 아래 두 파일의 SHA-256이 동일함을 확인했다.

- 저장소: `server/stt_worker.py`
- 설치본: `%LOCALAPPDATA%\Programs\voicecap-comment-helper\resources\app.asar.unpacked\server\stt_worker.py`
- SHA-256: `579845E8F4DB2F514BC68D3872079FF12BA2C043F703C71BC8C0EB4FDAFE6E6C`

따라서 Python 워커 분석은 설치된 워커 파일에도 적용된다. 설치본 전체 JS·웹 배포본의 동일성까지 검증한 것은 아니다.

### 격리 진단

실행 중인 워커에 테스트 음성을 보내거나 새 모델을 로드하지 않았다. 별도 프로세스에서 모델 추론을 모의 처리하고 실제 함수의 제어 흐름을 검증했다.

| 진단 | 결과 | 해석 |
| --- | --- | --- |
| 4096샘플짜리 완전 무음 14청크 입력 | 3.584초 무음이 `transcribe_pcm`에 전달됨 | 외부 구간 분할은 발화 없이도 강제 flush. 내부 Silero가 이후 제거할 수 있으므로 실제 환각 발생 증명은 아님 |
| 동일 길이의 연속 비무음 32청크 입력 | 3.584초 구간 2회, 1.024초 잔여 | 문장 경계와 무관한 절단 및 전체 버퍼 초기화 확인 |
| 0.256초 비무음 + 0.512초 무음 입력 | 0.768초 구간 전사 요청 | 최소 0.6초 조건이 실제 발화 길이가 아닌 전체 버퍼 길이에 적용됨 |
| 모의 결과 `가` 100회, avg_logprob=-0.1 | 100글자 전체를 `is_final=true`, confidence=0.90으로 전송 | 반복 품질 검증 없음 |
| 설치된 라이브러리의 실제 fallback 함수 + 모의 디코더 | compression ratio 18.75 > 기준 2.4인데 1회 디코딩 후 실패 후보 반환 | temperature=0.0 단일 후보는 반복 임계값 초과 시 재시도할 다른 온도가 없음. 임계값은 무조건 폐기 필터가 아님 |
| 브리지 바이너리→Base64→바이너리 | 테스트 바이트 완전 일치 | 해당 변환 자체에서 항상 PCM이 깨지는 버그는 발견하지 못함. 브라우저부터의 전체 입력 품질은 미검증 |
| 다른 소켓의 audio·stop 이벤트 | 소유권 검증 없이 워커에 전달됨 | 여러 탭/클라이언트에서 오디오 혼입·세션 간섭 가능 |
| READY 중 worker error 이벤트 | 브리지 상태는 READY 유지, error만 갱신 | 오류 상태 전파 결함 확인 |

기존 `node --test server/test/sttBridge.test.js`는 1개 테스트가 통과했다. 해당 테스트는 상태 객체·JSON 처리만 확인하며, 오디오·실제 인식·반복·실시간 지연을 검증하지 않는다.

## 3. 원인별 분석

### A. 선택 모델과 실제 모델이 달라질 수 있음 — 확인된 결함, 최우선

근거:

- `src/services/storageService.ts:322`: 저장값이 없으면 UI 모델 기본값은 large-v3-turbo.
- `server/stt_worker.py:227`: 워커는 항상 base/cpu/int8을 먼저 로드.
- `src/context/LiveContext.tsx:203`: select를 바꾸는 setter에서만 `loadModel(model)` 호출.
- `src/context/LiveContext.tsx:861`: 청취 시작 시 선택 모델을 워커에 적용하거나 실제 모델과 비교하지 않음.
- `src/services/localSttService.ts:119`: 모델 로드 기본 device는 cpu. UI에서 GPU를 자동 선택하는 경로 없음.

새로고침, 도우미 재시작 후 UI에는 저장된 small/Turbo가 선택되어 있어도 실제 엔진은 base일 수 있다. 현재 상태 API는 실제 base라고 보고했다. 사용자가 당시 어떤 모델을 선택했는지는 아직 확인되지 않았다.

개선:

1. requestedModel과 loadedModel을 분리한다.
2. 청취 요청에 원하는 model/device/computeType을 포함한다.
3. 선택 모델 로드 성공과 `listening_started` 응답을 확인한 후 실제 오디오 전송을 시작한다.
4. UI에 '요청 모델 / 실제 실행 모델'을 표시하고 불일치 상태에서는 준비 완료로 표시하지 않는다.
5. 모델 로드 실패 시 이전 모델을 사용하려면 이를 명시적으로 선택하게 한다. 조용한 fallback은 금지한다.

### B. 메모리 부족 및 실패한 모델 로드의 상태 처리가 잘못됨 — 실행 흔적과 코드로 확인

근거:

- 실행 중 상태 API에 `mkl_malloc: failed to allocate memory` 기록.
- `server/stt_worker.py:102`: 기존 모델 참조를 유지한 채 새 `WhisperModel(...)`을 생성 후 대입한다. 교체 순간 기존 모델과 새 모델의 메모리 수요가 겹칠 수 있다.
- `server/stt_worker.py:121`: 모델 실패 시 error 이벤트만 전송. 기존 모델·모델명은 남을 수 있다.
- `server/stt_worker.py:259`: start는 모델 준비 여부를 확인하지 않고 LISTENING으로 변경.
- `server/sttBridge.js:204`: error 이벤트가 `state='ERROR'`로 반영되지 않음.
- `src/services/localSttService.ts:66`: 청취 중이면 수신 상태 내용과 무관하게 CONNECTED 알림.
- `src/services/localSttService.ts:155`: 시작 확인 응답을 기다리지 않고 즉시 CONNECTED 처리.
- `server/stt_worker.py:174`: 전사 예외는 stderr만 출력. 사용자 화면으로 세션 오류 전달 없음.

영향: 모델이 준비되지 않았거나 실패해도 사용자는 정상 청취로 볼 수 있다. 실제 모델이 None이면 전사 함수는 아무 결과 없이 반환한다. 모델이 이전 base로 남으면 선택과 다른 결과를 받는다.

개선:

- DISCONNECTED→LOADING→READY→STARTING→LISTENING 및 ERROR 전이를 응답 기반으로 관리.
- 로드·전사 오류에 error code, 실패한 model, request/session ID, 발생 시각 포함.
- 성공 시 지난 오류를 정리하고, 현재 오류와 오류 이력을 구분.
- 같은 모델이면 재로드하지 않음. 교체 시 새 프로세스와 기존 프로세스 동시 적재가 가능한지 확인하고, 저메모리 PC에서는 기존 모델 종료 후 새 모델 로드.
- 대형 모델은 설치 가능 여부와 실제 실행 가능 여부를 구분해 진단.
- Python·라이브러리 버전을 앱에서 관리하고 특정 개발 도구의 venv에 의존하는 배포를 정리.

### C. 짧은 고정 절단과 문맥 단절 — 확인된 결함, 인식 저하의 유력 요인

근거:

- `server/stt_worker.py:34-37`: RMS 0.012, 무음 0.5초, 최대 버퍼 3.5초.
- `server/stt_worker.py:188-214`: 모든 청크를 누적하다 최대 길이에 도달하면 문장 중간이어도 버퍼를 비움.
- 브라우저 청크는 4096 / 16000 = 0.256초이므로 연속 음성의 실제 절단 주기는 대략 3.584초.
- 앞뒤 겹침, 이전 발화 문맥, 중간 자막 안정화 없음. 모든 청크 결과가 확정값으로 나감.
- `condition_on_previous_text=False`인데 호출 자체도 매번 독립되어 이전 문장 문맥이 전달되지 않음.

한국어 닉네임·숫자·명령의 어절이 구간 경계에서 나뉘면 인식과 업무 파싱 모두 손상될 수 있다. 현재 구조는 '짧은 파일을 연속 전사'하는 형태이며 안정적인 스트리밍 확정 처리가 구현된 상태가 아니다.

개선:

- 실제 스트리밍 VAD로 발화 시작/끝을 먼저 판단.
- 음성 시작 전 약 200~300ms pre-roll, 끝부분 여유 구간, 최대 발화 길이를 둔다. 수치는 초기 실험값이다.
- 강제 분할은 무음 또는 안전한 경계를 우선하고 필요한 겹침을 보존한다.
- 겹친 결과는 오디오 시각과 안정된 텍스트 접두부를 기준으로 병합한다.
- 긴 문장에서는 중간 자막과 확정 텍스트를 분리하고 확정된 추가분만 업무 처리한다.
- J4105에서는 과도한 재추론을 피한다. 먼저 발화 단위 전사로 기준 품질을 확보한 뒤 중간 자막 빈도를 정한다.
- 단순히 `condition_on_previous_text=True`로 바꾸는 것은 호출 간 문맥을 복원하지 못하며 반복 루프를 악화시킬 수 있으므로 우선 처방으로 삼지 않는다.

### D. 반복 생성 결과를 걸러내지 않음 — 확인된 결함, 반복 증상의 직접적인 통과 경로

근거:

- `server/stt_worker.py:140-150`: beam_size=1, temperature=0.0, best_of=1.
- 라이브러리의 compression ratio 검사는 fallback 판단에 사용된다. 0.0 단일 온도에서는 다른 후보를 시도하지 못하며 최종 실패 후보가 반환될 수 있다.
- `server/stt_worker.py:155-172`: 비어 있지 않은 모든 텍스트를 합쳐 확정 이벤트로 전송한다.
- segment의 compression_ratio/no_speech_prob/생성 길이에 대한 앱의 최종 검증 없음.
- `exp(avg_logprob)`는 해당 발화의 한국어 정확도를 보장하는 점수가 아니다. 반복 문장도 높은 값으로 통과할 수 있다.

개선 순서:

1. 결과에 음성 길이, 유효 음성 길이, avg_logprob, no_speech_prob, compression_ratio, 토큰 수, 추론 시간을 기록한다.
2. 글자·어절 연속 반복, 비정상 압축률, 음성 길이에 비해 과도한 출력 길이를 조합해 비정상 결과로 분류한다.
3. 비정상 결과는 판매·화면 캡처·라벨 인쇄 등 업무 동작에 넘기지 않는다.
4. 원래 오디오가 유효하면 제한된 횟수로 프롬프트 제거 또는 다른 디코딩 설정으로 재시도하고, 계속 실패하면 '인식 실패'로 남긴다.
5. 무한 재시도와 반복 텍스트를 몇 글자로 잘라 정상 결과처럼 사용하는 방식을 피한다.
6. beam_size=3~5, 제한된 온도 fallback, repetition_penalty, no_repeat_ngram_size는 별도 A/B 실험한다. 이 PC에서는 지연·숫자 반복 손상 가능성을 함께 확인한다.

공식 근거: [faster-whisper v1.2.1 transcribe.py](https://github.com/SYSTRAN/faster-whisper/blob/v1.2.1/faster_whisper/transcribe.py). 특히 `generate_with_fallback`와 `TranscriptionOptions`를 참조한다.

### E. 인계서의 VAD 설명과 실제 구간 분할이 다름 — 확인된 구현 차이

외부 실시간 구간 분할은 Silero가 아니라 RMS 기준이다. Silero는 이미 잘린 구간을 `transcribe(vad_filter=True)`에 넘긴 뒤 내부에서 적용된다. 'Silero가 전혀 없다'는 뜻은 아니며, 현재 역할을 구분해야 한다.

RMS는 말소리와 배경음악·잡음을 구분하지 못한다. 일정 시간이 되면 발화 여부와 관계없이 전사 함수를 호출한다. 최소 발화 길이 검사는 실제 말한 길이가 아닌 무음 포함 전체 버퍼 길이로 동작한다.

개선: 전사 전 VAD로 유효 음성 구간을 형성하고 무음 구간을 제외한다. 이때 기존 내부 VAD와 이중 절단이 발생하지 않도록 옵션을 설계한다. 낮은 볼륨 때문에 누락되는지 확인한 뒤 입력 레벨을 조정하며, 일괄 증폭으로 배경 잡음을 키우지 않는다.

### F. 추론이 입력·중지 명령 처리를 막고 큐가 무제한으로 쌓일 수 있음 — 확인된 구조적 위험

Python은 한 루프에서 stdin을 읽다가 동기적으로 전사를 수행한다. 추론하는 동안 다음 audio/stop 명령은 읽지 못한다. Node의 `stdin.write()` 반환값과 drain을 처리하지 않고 큐 길이 제한도 없다.

예를 들어 3.584초 오디오 추론이 5초 걸린다면 매 구간 약 1.416초씩 뒤처진다. 이는 설명용 예시이며 현재 PC의 실측 추론 시간이 아니다. 고부하에서는 자막이 한참 늦게 나타나거나 중지가 늦어질 수 있다.

개선:

- 입력·제어와 추론 스케줄링을 분리하고 처리 중인 요청의 취소 정책을 설계.
- 대기 오디오 초, 가장 오래된 청크 나이, 추론 초/오디오 초(RTF), dropped samples를 계측.
- 제한된 큐와 역압(backpressure)을 적용하고 과부하를 UI로 알림.
- RTF<1을 지속 충족하는지 검증. 중간 자막 재추론을 사용하면 전체 작업량 기준으로 다시 측정.
- 조용히 청크를 버리지 않고 유실 구간을 기록하며 결과 신뢰성을 낮춰 표시.

### G. 여러 클라이언트의 오디오 혼입 및 세션 간섭 가능 — 조건부 원인

오디오 프레임에 세션/세대/순번이 없고 서버에 소유 소켓 검증이 없다. 어느 연결이든 audio/start/stop을 보낼 수 있으며 Python 전역 버퍼는 하나다. 결과도 전체 소켓에 브로드캐스트한다.

프런트엔드의 session/generation 필터는 다른 결과를 화면에서 버릴 수 있지만, 워커 입력 단계의 혼입을 복구하지 못한다. 여러 브라우저 탭이 동시에 청취한 경우 왜곡될 수 있다. 실제 동시 청취 여부는 아직 확인되지 않았다.

개선: 단일 청취 소유권 또는 세션별 워커를 명시하고, 시작·오디오·중지에 sessionId/generation/sequence를 포함한다. 소유 소켓이 아닌 요청은 거절하고 결과는 해당 세션에만 전송한다. 재접속 시 낡은 audio/start/stop을 재사용하지 않는다.

### H. 실제 원본 오디오 품질은 아직 확인되지 않음 — 중요한 미확인 항목

코드상 브라우저는 AudioContext 16kHz를 요청하며 PCM16 변환식과 Windows의 int16 해석은 맞는다. 따라서 '48kHz를 무조건 16kHz로 잘못 읽고 있다'고 단정할 근거는 없다.

다만 실제 탭 선택, 음소거, 작은 볼륨, 배경음악 비중, 왜곡, 프레임 누락은 파형 표시만으로 확인할 수 없다. 화면의 볼륨 값은 주파수 분석값에서 계산되며 STT 입력 PCM의 품질 보증이 아니다. ScriptProcessor의 메인 스레드 의존성과 오디오 초기화 예외를 경고만 출력하고 계속 진행하는 동작도 점검 대상이다.

개선 진단:

- 사용자가 제공하거나 진단용으로 선택한 10~30초 입력을 브라우저 원본 PCM과 워커 수신 PCM 양쪽에서 WAV로 저장.
- sampleRate/channels/format/sequence/sample count를 함께 기록하고 양쪽 바이트 또는 파형을 비교.
- 직접 재생하여 사람 목소리가 선명한지 확인하고 peak/RMS/클리핑/무음 비율을 계산.
- 동일 WAV의 파일 전사가 양호한데 라이브만 나쁘면 스트리밍 구간·큐·전송 문제를 우선 수정.
- 파일 전사도 나쁘면 모델·입력 음질·VAD·프롬프트 조건을 비교.
- 실제 누락을 확인하거나 수집 구조를 개선할 때 AudioWorklet과 명시적인 리샘플링·메타데이터 검증을 도입.

## 4. 권장 개선 순서

| 순서 | 작업 | 완료 판단 |
| --- | --- | --- |
| 1 | 요청 모델/실제 모델 일치, 오류 전달, 준비 완료 ACK | 실패 상태에서 청취 성공 표시가 사라지고 실제 model/device를 확인할 수 있음 |
| 2 | 입력 WAV 및 처리 로그 확보, 비정상 반복 결과 차단 | 입력 품질 비교 가능. 반복 100회 모의 결과가 업무 처리에 도달하지 않음 |
| 3 | 진짜 발화 기반 구간 분할, 겹침/확정 처리 | 문장 경계·닉네임·금액 연결 및 반복 발화 테스트 통과 |
| 4 | 큐 상한, 취소, 세션 소유권 | 과부하·중지·재시작·다중 탭에서 오디오 혼입과 늦은 업무 실행 없음 |
| 5 | 동일 WAV로 base/small/Turbo 또는 대안 비교 | CER, 금액/닉네임 일치율, RTF, 지연, 메모리를 함께 기록 |
| 6 | 실제 방송 조건 장시간 검증 및 패키지 재배포 | 대상 PC에서 충분한 품질·속도 확인 후 배포 |

각 단계의 단순 테스트 통과를 한국어 인식률 검증으로 대체하지 않는다.

## 5. 현재 PC에 맞춘 모델 전략

- base는 파이프라인 진단을 위한 가벼운 기준 모델로 사용하되 한국어 업무용 정확도 권장이라는 문구는 실측 전 제거한다.
- small int8을 다음 비교 후보로 삼는다. 현재 가용 메모리가 작으므로 기존 모델을 겹쳐 로드하지 않고 방송·브라우저 동시 실행 조건에서 검사한다.
- large-v3-turbo는 현재 PC의 기본 처방으로 삼지 않는다. GPU 지원과 메모리 여유를 확보한 환경에서 별도로 비교한다.
- 현재 코드는 CPU를 기본값으로 고정한다. 드롭다운에서 Turbo를 선택해도 자동 GPU 실행이 되지 않는다.
- Whisper.cpp/OpenVINO 또는 다른 런타임 최적화는 동일한 올바른 입력을 기준으로 성능 비교한다. 실행 엔진을 바꿔도 잘린 오디오·모델 불일치·반복 결과 통과는 자동으로 고쳐지지 않는다.
- Qwen 등 모델 교체는 이후 비교 단계다. 이 PC에서 인식률과 실시간 속도를 모두 만족하는 무료 로컬 구성이 나올지는 아직 검증되지 않았다.
- 목표 품질을 충족하지 못하면 고성능 PC에서 로컬 실행하거나 기존 클라우드 모드를 선택하는 운영 방안을 명확히 안내한다.

## 6. 인계인수서 수정 필요 사항

`HANDOFF.md`는 작업 개요 파악에는 유용하지만 검증 결과로 그대로 신뢰할 수 없는 항목이 있다.

1. '한국어 실시간 추론 레이턴시 0.3~0.7초 달성': 측정 파일·장비·방법·로그가 문서에 없으며, 현재 연속 발화 버퍼는 약 3.584초에야 첫 전사 요청을 보낸다. 순수 추론 시간과 음성부터 자막까지의 전체 지연을 구분해야 한다.
2. 'Silero VAD 기반 실시간 자동 분할': 실제 외부 분할은 RMS이며 Silero는 전사 내부 필터다.
3. '하트비트': 워커에는 ping 명령이 있으나 브리지에 주기 ping/timeout 감시 구현이 없다.
4. '완전 동일 환경': 시스템 Python 및 특정 Hermes venv에 의존하므로 설치 EXE만으로 동일 STT 환경이 보장되지 않는다.
5. `npm run build:win`: 현재 도우미 package.json에는 없고 `dist`, `pack`, `stage` 등이 있다.
6. 기술 스택 표의 Vite 5/Electron 35와 현재 package.json의 Vite 6/Electron 44가 다르다.
7. 테스트 통과는 STT 실사용 검증을 의미하지 않는다. 오디오 기반 정확도·장시간 실행 테스트가 필요하다.

이 분석에서는 기존 인계서를 덮어쓰지 않았다. 개선 구현 후 실제 결과에 맞게 갱신해야 한다.

## 7. 다음 구현 AI에게 전달할 요청

> 이 보고서와 `OFFLINE_STT_IMPLEMENTATION_PLAN.md`를 읽고 로컬 STT를 개선해라. 먼저 요청 모델과 실제 로드 모델을 일치시키고 준비 완료 응답·오류 전파를 수정해라. 원본 PCM과 워커 입력을 비교할 진단 경로를 만든 뒤, 한 글자 반복 결과가 확정 업무 처리로 넘어가지 않도록 차단해라. 다음으로 발화 기반 VAD, 구간 병합·확정, 큐 상한 및 취소, 세션 소유권을 구현해라. 현재 PC는 Celeron J4105/UHD600/약 8GB이고 메모리 부족 기록이 있으므로 큰 모델로 무조건 변경하지 마라. 실제 한국어 WAV로 품질과 속도를 함께 측정하고 mock 테스트와 실제 인식 테스트 결과를 구분해서 보고해라. 기존 클라우드 STT·탭 공유 유지·판매·캡처 동작을 보존하고, 운영 배포는 해당 사용자 요청 범위에 따라 진행해라.

## 8. 공식 참고 자료

- [faster-whisper v1.2.1 전사·디코딩 코드](https://github.com/SYSTRAN/faster-whisper/blob/v1.2.1/faster_whisper/transcribe.py)
- [faster-whisper v1.2.1 Silero VAD 코드](https://github.com/SYSTRAN/faster-whisper/blob/v1.2.1/faster_whisper/vad.py)
- [faster-whisper 실행 조건 및 벤치마크](https://github.com/SYSTRAN/faster-whisper)

공식 벤치마크의 다른 CPU/GPU 처리 시간을 J4105의 한국어 라이브 처리 성능으로 사용하지 않는다.
